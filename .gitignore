.build/
.DS_Store
vendor/llama.cpp/
vendor/azooKey_dictionary_storage/
vendor/dist/
# 打ち間違い訂正モデル（install-typo-normalizer.sh が書き出しから作る）。
# /export-scale-16x/ は学習機からの書き出しの置き場所。どちらも重みなのでgitには入れない
vendor/typo-normalizer/
/export-scale-16x/
vendor/dist-windows/
vendor/dist-windows-vulkan/
windows/**/build/
windows/build-vulkan/
*.gguf
.venv/
# 配布用にまとめたモデル（publish-typo-normalizer.sh の出力。Releaseへ上げるだけで追跡しない）
/dist/

# 学習まわり（training/）。2026-09-22 にリポジトリから外した（それまでは
# スクリプト10件だけ追跡していた）。prepare_data.py が作るデータの出所が
# zenz-v2.5-dataset なので、iroha のリポジトリから他者のデータを再配布しないため。
# 中身は履歴に残っている: git checkout v0.12.0 -- training
/training/
testdata/ajimee/
# 実験ディレクトリ。2026-09-22 にリポジトリから外した（追跡していた23件も削除）。
# 学習データ・チェックポイントが大きいうえ、jev/ajimee-candidates*.jsonl（AJIMEE-Bench、
# CC-BY-SA 3.0）や typo-normalizer/results/ の実文など他者のデータを含むため。
# 中身は履歴に残っている: git checkout v0.12.0 -- experiments
/experiments/

# 助成金申請書類（非公開）
grants/
experiments/**/__pycache__/
